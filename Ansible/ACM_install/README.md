# ACM Operator Installation
This folder contains the needed ansible files to install the ACM operator atop an Openshift cluster.

Essentially, running the `install_rhacm.yaml` play will utilize the associated files residing within the `rhacm_files` dir, and apply them.  
